// Composes read models directly via each module's index.ts — no owned schema, no gateways.
// TODO: take other modules' services as dependencies as their read models come online.
export class DashboardsService {
  // TODO: business logic
}
